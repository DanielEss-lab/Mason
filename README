#openbabel, tqdm, xtb(linux only) required
conda create -n mason
conda activate mason
conda install -c conda-forge openbabel
conda install -c conda-forge xtb
conda install tqdm

# TO generate your own structures by modifing everything in Pt_Methane.ini file 
# and modify the configFile in "main.py" and "runxtb.py" as well if you change the configuration filename.
# Excute "python main.py" and locate your generated sturctures at output/xtbopt/